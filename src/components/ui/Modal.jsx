// Styles: 
import { Container, Wrapper } from './Modal.styles';

const Modal = ({ children }) => {
  <Container>
    <Wrapper>
      { children }
    </Wrapper>
  </Container>
};

export default Modal;